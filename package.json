{
  "name": "scalper",
  "description": "An authentication ticket store designed to allow using an express app to do auth for a socket.io app",
  "author": "Joe Wagner",
  "version": "1.0.0",
  "scripts": {
    "test": "NODE_ENV=test mocha --exit"
  },
  "main": "index.js",
  "dependencies": {
    "uuid": "^8.2.0"
  },
  "devDependencies": {
    "mocha": "^8.0.1",
    "should": "~3.3.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:JoeWagner/scalper.git"
  }
}
